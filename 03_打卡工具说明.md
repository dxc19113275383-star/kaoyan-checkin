# 打卡工具说明（供后续迭代）

> 最后更新：2026-06-25 · 版本 v6.5（作文模块 · P5）

---

## 一、文件说明

| 文件 | 用途 |
|------|------|
| `index.html` | **主应用**（PWA 版），部署到静态服务器后可通过浏览器打开 |
| `manifest.json` | PWA 清单，定义应用名称、图标、全屏模式等 |
| `icon.svg` | 矢量应用图标（"研"字 + 深蓝渐变背景），一份文件适配所有尺寸 |
| `sw.js` | Service Worker，实现离线缓存，首次访问后断网也能用 |
| `打卡工具.html` | 旧版（保留），双击即可本地打开，仍可用但不含新功能 |

> **PWA 特性**：支持 iOS Safari "添加到主屏幕"、Android Chrome 安装提示、离线使用。

---

## 二、数据结构（v4）

```js
state = {
  // === v1 字段（保持兼容）===
  p: 0, wk: 1, day: 0,
  data: {},                 // 打卡记录

  // === v2 字段 ===
  startDate: "2026-06-01",
  examDate: "2027-12-25",
  customTasks: {},
  deletedDefaults: {},

  // === v3 字段 ===
  notes: {},                // 每日笔记：键=YYYY-MM-DD

  // === v4 字段 ===
  resources: [],            // 资料包
  studySessions: [],        // 学习会话
  aiChats: [],              // AI 对话记录

  // === v5/v6 字段 ===
  reminderSettings: {...},  // 学习提醒
  reminderLog: {},          // 提醒日志
  notificationSettings: {...}, // Web Push 推送设置

  // === v7 字段：学习中心 ===
  learn: {
    vocab: {                      // 背单词（百词斩底层逻辑）
      active: "kaoyan",           // 当前词库 id
      settings: { dailyNew: 20 }, // 每日新词配额
      progress: {                 // 键=`${libId}:${word}`
        "kaoyan:abandon": { stage:3, due:"2026-06-28", wrong:0, seenAt:"..." }
      },
      wrongBook: ["kaoyan:abandon"], // 错词本（key 列表）
      daily: { date:"2026-06-24", newDone:5, reviewDone:12 }, // 当日进度（跨天重置）
      userLibs: [ { id:"user_xxx", name:"我的专业英语", count:120 } ] // 导入词库索引
    },
    reading: {                    // 阅读（薄荷阅读式精读）
      progress: { "comparative-advantage": { read:true, readAt:"...", saved:3 } },
      userPassages: [ { id:"u_xxx", title:"我的真题阅读" } ] // 导入文章索引
    },
    math:    {                    // 数学（396 分类练习 + 错题本）
      active: "calc",             // 当前题集 id
      progress: {                 // 键=`${setId}:${qid}`
        "calc:c1": { stage:2, correct:true, wrong:0, seen:3, lastAt:"..." }
      },
      wrongBook: ["calc:c3"],     // 错题本（key 列表，连续答对达 MASTER 后移除）
      daily: { date:"2026-06-25", done:8, correct:6 }, // 当日进度（跨天重置）
      userSets: [ { id:"user_xxx", name:"我的真题·微积分", count:30 } ] // 导入题集索引
    },
    exams:   { progress:{} },     // 真题题库
    mock:    { history:[] }       // AI模拟考记录
  },
  version: 7
}
```

> **词库内容存储**：bundled 词库放仓库 `data/words/*.json`（注册表 `data/words/index.json`），按需 fetch + SW 缓存离线；用户导入词库存 `localStorage['vocab_lib_<id>']`（不进 state，避免膨胀）。`state.learn.vocab` 只存进度/设置，并入导出备份。
>
> **词库 JSON 格式**：`{ id, name, desc, words:[ {w, ph, def, ex, exCn} ] }`，导入时也接受裸数组。`w`(单词)必填，其余可选。

> **题集 JSON 格式**：`{ id, name, cat, questions:[ {id, cat, level, stem, options:[...], answer, explain} ] }`，导入时也接受裸数组。`stem`(题干)/`options`(选项数组)/`answer`(正确项下标,从0起)必填，`explain`(步骤讲解)/`cat`/`level` 可选。bundled 题集放 `data/math/*.json`（注册表 `data/math/index.json`）；用户导入题集存 `localStorage['math_set_<id>']`（不进 state）。

> **存储原则**：内容（词库/题库等只读大文件）走仓库 `/data/` 下静态 JSON，按需 fetch + SW 缓存离线；`state.learn` 只存**进度/设置**（体积小），并入现有导出/导入备份。`defaultLearn()` 提供默认结构，`migrate()` 中 v6→v7 自动补齐、且做了字段缺失防御。

### resources 对象结构
```js
{
  id: "r_1719000000_abc12",
  subject: "math",           // eng|math|logic|write|major|pol|other
  category: "网课",
  type: "video",             // video|pdf|doc|link|image|folder
  title: "张宇396数学基础课",
  url: "https://...",
  provider: "B站",
  panCode: "a1b2",          // v4.4: 网盘提取码（仅网盘链接用，可选）
  linkedTaskTags: ["t-math"],
  stage: null, month: null, week: null,
  progress: { status:"studying", percent:30, lastPositionText:"看到第5节", lastStudiedAt:"...", videoTime:128.5 }, // v4.4: videoTime=视频续播秒数
  note: "",
  createdAt: "...", updatedAt: "..."
}
```

### studySessions 对象结构
```js
{
  id: "r_...",
  resourceId: "r_...",      // 关联资源（可为 null）
  taskKey: null,
  subject: "math",
  title: "张宇396数学基础课",
  startedAt: "...", endedAt: "...",
  minutes: 20,
  result: "completed",      // completed|partial|not_done
  note: ""
}
```

### notes 对象结构

```js
// 键为 YYYY-MM-DD 日期字符串（由 startDate 推算得出）
"2026-06-23": {
  weakSubject: "math",                      // 薄弱科目
  text: "排列组合公式记混了，需要重做例题",   // 笔记正文（≤100字）
  createdAt: "2026-06-23T14:30:00.000Z"     // ISO 时间戳
}
```

### weakSubject 枚举

| 值 | 含义 |
|----|------|
| `eng` | 英语 |
| `math` | 数学 |
| `major` | 专业课（434） |
| `logic` | 逻辑/写作 |
| `write` | 写作 |
| `pol` | 政治 |
| `other` | 其他 |

### 迁移机制

- v1（无 version）→ 自动补 v2 + v3 字段
- v2（version=2）→ 自动补 notes + 升级 version=3
- 旧 notes 如使用 p/w/d 键格式，首次访问时会按日期键重写（兼容过渡期数据）

---

## 三、核心配置

### 阶段一·在职（8个月/32周）
- `P0_MONTHS[8]` — 每月目标 + 聚焦点
- `P0_WEEKDAY[4]` — 工作日任务模板
- `P0_SAT[4]` — 周六任务
- `P0_SUN[4]` — 周日任务

### 阶段二·全职（11个月/44周）
- `P1_MONTHS[11]` — 每月目标 + 聚焦点
- `P1_WEEKDAY[5]` — 工作日任务（周一~周六同结构）
- `P1_SUN[4]` — 周日任务（含模考+休息）

---

## 四、任务对象字段说明

```js
{
  label: "任务文字",
  tag: "t-eng",        // 色块样式类
  tagText: "英语",      // 色块显示文字
  focusKey: "word",    // 可选：从当月对象取该字段作为聚焦提示
  focusPre: "今日背：", // 可选：聚焦提示前缀
  focus: "固定提示文字"  // 可选：直接写死的聚焦提示
}
```

---

## 五、色块类（tag）

| 类名 | 含义 | 配色 |
|------|------|------|
| t-eng | 英语 | 蓝 |
| t-math | 数学 | 琥珀 |
| t-rev | 回顾/休息 | 青 |
| t-logic | 逻辑/写作 | 紫 |
| t-write | 写作 | 粉 |
| t-major | 专业课/模考 | 珊瑚 |
| t-pol | 政治 | 红 |

---

## 六、关键逻辑函数

### 基础函数
- `keyFor(p, wk, day)` — 阶段+周+天 → 存储键 `"p0w1d0"`
- `dateFor(p, wk, day)` — 阶段+周+天 → `YYYY-MM-DD` 绝对日期（需设置 startDate）
- `todayStr()` — 返回今天的 `YYYY-MM-DD`
- `monthIdxFor(p, wk)` — 阶段+周 → 第几个月（每月4周）
- `tasksFor(p, dayIdx)` — 取当天任务模板
- `effectiveTasks(p, wk, day)` — 取实际任务列表（自定义 > 默认，过滤隐藏项）
- `focusText(t, m)` — 计算聚焦提示文字
- `computeStreak()` — 连续打卡天数
- `computeTotal()` — 累计完成项数

### 笔记闭环
- `getNote(dateKey)` — 按 YYYY-MM-DD 读取笔记
- `saveNote(dateKey, weakSubject, text)` — 保存笔记
- `renderNotesHistory()` — 渲染最近5条笔记回看
- `renderYesterdayReminder()` — 渲染昨日薄弱点提醒（含3天连续同科警告）

### 分析与可视化
- `getTagStats(daysBack)` — 统计最近N天各 tag 完成率
- `renderBiasWarning(tasks, done)` — 偏科预警（阈值见下方规则）
- `heatmapColor(cnt, total)` — 热力图5色映射
- `renderHeatmap()` — 渲染4周热力图

### 阅读引擎（v4.7 · 薄荷阅读底层逻辑）
**核心**：精读文章 + 点词查义 + 一键加入背单词（与背单词引擎打通）+ 已读/生词进度。
- `loadReadingList()` / `loadPassage(p)` — 读注册表 `data/reading/index.json` 与文章 JSON；用户导入文章存 `localStorage['reading_<id>']`
- `openReading()` / `renderReadingHome()` — 文章列表 + 已读数 + 导入入口
- `openPassage(id)` — 加载文章，构建查词表 `_readingDict`（本文 glossary + 当前背单词库合并）
- `rdTokenize(text)` — 把段落里每个英文单词包成可点击 `<span class="rd-word">`
- `readingTap(el)` — 点词弹层：释义（查 `_readingDict`，含简单去屈折回退）+ 🔊TTS + 「加入背单词」
- `addWordToReadingLib(entry)` — **关键打通**：把生词写入 `localStorage['vocab_lib_reading']` 并注册为背单词 userLib「阅读生词本」，直接进入 SRS 复习
- `markPassageRead(id)` — 标记已读（可取消）
- **文章 JSON 格式**：`{ id, title, source, level, intro, paragraphs:[...], glossary:[{w,ph,def,ex,exCn}] }`，glossary 可选
- **内容**：附 2 篇原创短文（国际贸易/跨境电商，贴合 MIB，规避版权）；真题/外刊用「导入文章」粘 JSON 加入
- **已知取舍**：生词词条存 localStorage（不进 state/导出备份），换设备后 SRS 进度键会失去对应词条（无害）；与背单词导入词库同一架构

### 数学引擎（v4.8 · 396 分类练习）
**核心**：按题集（微积分/线代/概率）做单选题 + 即时判分 + 内置步骤讲解 + 错题本重做 + AI 详解（仅辅助）。
- `MATH_MASTER = 3`（同题连续答对累计达此 stage = 已掌握，自动移出错题本）
- `ensureMath()` — 取/补 `state.learn.math` 字段；`daily` 跨天自动重置
- `loadMathSets()` — 读注册表 `data/math/index.json` + 合并用户导入题集
- `loadMathQuestions(setId)` — 取题表：用户导入(localStorage)优先，否则 fetch `data/math/<file>`，缓存于 `_mathCache`
- `openMath()` / `renderMathHome()` — 题集选择器 + 题目数/正确率/已掌握三数 + 进度条 + 开始练习/错题本/导入
- `buildMathQueue(qs, mode)` — `practice`=全题集，`wrong`=错题本；洗牌
- `renderMathCard()` — 分类标签 + 题干(`.math-stem`) + A/B/C/D 选项（复用 `.vocab-opt`）
- `mathPick()` / `gradeMath()` — 判分：对→`stage+1`、错→`stage=0`+入错词本；记当日 done/correct
- `showMathFeedback()` — 正误 + 正确答案 + 内置 `.math-explain` 步骤讲解 + 「🤖 AI 详解」「下一题」
- `mathAskAI()` — **AI 辅助打通**：把题干+选项+正确项填入 AI 助手输入框、切到「错题讲解」模式、跳转 AI Tab，由用户发送（AI 仅辅助，注意核对）
- `mathOpenImport()` / `mathImpSave` — 导入题集（粘贴 JSON）
- **内容**：附 3 个种子题集 `data/math/{calc,linear,prob}.json`（微积分/线性代数/概率统计各 8 题，含步骤讲解）；逻辑题集注册为「待导入」占位。真题用「导入题集」粘 JSON 加入。
- **已知取舍**：题干用纯文本/Unicode 写数学符号（无 MathJax 依赖，保证离线零依赖）；复杂公式以可读文字描述。AI 详解走在线代理，离线时仅内置 explain 可用。

### 背单词引擎（v4.6 · 百词斩底层逻辑）
**核心机制**：斩词识别（看单词选释义 4 选 1）+ 艾宾浩斯间隔复习 + 每日新词配额 + 熟词跳过 + 错词本。
- `VOCAB_INTERVALS = [0,1,2,4,7,15,30,90,180]`（天）；`VOCAB_MASTER_STAGE = 7`（达到=已掌握）
- `ensureVocab()` — 取/补 `state.learn.vocab` 字段；`daily` 跨天自动重置
- `loadVocabLibs()` — 读注册表 `data/words/index.json` + 合并用户导入词库
- `loadVocabWords(libId)` — 取词表：用户导入(localStorage)优先，否则 fetch `data/words/<file>`，缓存于 `_vocabCache`
- `openVocab()` / `renderVocabHome()` — 词库选择器 + 今日新词/待复习/已掌握三数 + 进度条 + 开始/错词本/每日配额
- `buildVocabQueue(words, mode)` — `today`=待复习+新词(配额内)，`wrong`=错词本；洗牌
- `renderVocabCard()` — 单词大字 + 音标 + 🔊TTS(`vSpeak`，Web Speech API) + 4 释义选项 + 「我认识」
- `vocabPick()` / `gradeVocab()` — 判分：对→`stage+1`、错→`stage-2`+入错词本；按 `stage` 取间隔算 `due`；记当日计数
- `vocabKnown()` — 熟词一键标记掌握；`vocabImpSave` — 导入词库（粘贴 JSON）
- **图省略说明**：百词斩的"配图"是逐词版权资产，无法离线/合规凑齐，故用文字卡替代，但记忆算法与交互循环一比一还原。
- **内容**：附种子库 `data/words/kaoyan.json`（考研核心 50 词，含大量商务/经济词）；四级/六级/专业英语注册为"待导入"占位。

### 学习中心框架（v4.5）
底部 `资料包` Tab 改名为 `学习`（仍 3 Tab）。「学习」是一个**两层结构**：hub 卡片网格 + 子页内路由（不走底部导航的 showPage，而是 tab 1 内部切换）。
- `LEARN_MODULES[]` — 模块配置数组（key/icon/title/sub/ready/todo），同时驱动 hub 卡片与占位页文案
- `renderLearnHub()` — 按 LEARN_MODULES 渲染卡片网格；`ready:true` 显示「可用」、其余「敬请期待」
- `showLearnView(name)` — tab 1 内部路由：`hub`(中心) / `resources`(资料网课，复用查看器) / 其余 key → 通用占位页 `pageLearnTodo`
- 页面 id：`pageLearn`(hub) / `pageLearnTodo`(占位) / `pageResources`(资料网课)
- `showPage(1)` 进入即 `showLearnView('hub')`；各子页有「← 返回学习」回 hub
- **当前状态**：框架/导航/数据模型已就位；`资料/网课` 已可用，其余 6 个引擎为占位，逐个填充不动框架

### 资料包应用内查看器（v4.4）
- `detectPlayMode(r)` — 按链接判定播放方式：`pan`(网盘) / `bili`(B站) / `hls`(.m3u8) / `video`(.mp4等直链) / `pdf` / `office`(Word/PPT) / `image` / `extvideo`(非直链视频) / `link` / `none`
- `openViewer(r, mode)` — 打开应用内查看器弹窗，按 mode 渲染 `<video>` / iframe / Office Online / B站播放器
- `closeViewer()` — 关闭并保存进度，弹出学习会话确认
- `_saveViewerProgress()` — 把视频 `currentTime` 写入 `progress.videoTime`，并据时长算 `percent`（≥95% 标记 done）
- `loadHls(cb)` — 仅在遇到 `.m3u8` 时懒加载 hls.js（CDN），不影响离线
- `copyText(t)` — 复制提取码到剪贴板（网盘链接打开时自动调用）
- `escAttr(s)` / `fmtTime(s)` — iframe 属性转义 / 秒数转 `分:秒`

**路由规则**：`pan`/`link`/`extvideo` → 跳转新窗口（网盘自动复制提取码）；其余可内嵌类型 → 应用内查看器。视频支持记忆进度、重开续播。卡片显示 `▶ 在线播放` 或 `↗ 跳转` 角标。

> **依赖直链**：PDF/Office 内嵌、视频播放都需要「可公开访问的直链」。对象存储私有桶用签名 URL（有有效期）；百度网盘等不提供直链，只能跳转。

### 导入导出
- `exportData()` — 导出 JSON（含 notes）
- `importData()` — 导入 JSON（含格式校验）
- `migrate(loaded)` — 自动数据迁移（`panCode`/`videoTime` 为可选字段，旧数据无需迁移）

---

## 七、偏科预警规则

### 冷启动保护
- 累计有打卡数据的天数 < 3 → 不显示预警，改为引导文案：`📊 完成 3 天后开始生成偏科趋势`
- 最近 7 天内某科任务总数 < 2 → 该科不参与预警

### 统计范围
只统计考试科目：`t-eng`（英语）、`t-math`（数学）、`t-major`（专业课）、`t-logic`（逻辑/写作）、`t-write`（写作）、`t-pol`（政治）。
**不统计** `t-rev`（回顾/休息）。

### 阈值
| 条件 | 级别 | 视觉 |
|------|------|------|
| 该科最近7天总任务 < 2 | 不参与预警 | — |
| 完成率 < 25% | 红色预警 🔴 | 红色 replan 卡片 |
| 完成率 < 40% | 黄色预警 🟡 | 黄色 replan 卡片 |
| 连续两周 < 40%（仅第2周起） | 连续两周偏低标签 | 红色标签附加 |

### 预警数量限制
- 首页最多展示 **2 条**预警
- 优先级：红色（<25%）> 连续两周预警 > 黄色（<40%）
- 超出 2 条时显示：`还有 X 项提醒未展开`

### 预警文案
每条预警包含**动作建议**，每科有独立的建议文案。

---

## 八、热力图颜色规则

| 颜色 | 含义 | CSS 类 |
|------|------|--------|
| 灰色 | 当天无任务（无记录） | `hm-gray` |
| 红色 | 有任务但完成率 0% | `hm-red` |
| 黄色 | 完成率 1%–59% | `hm-yellow` |
| 绿色 | 完成率 60%–89% | `hm-green` |
| 深绿色 | 完成率 90%–100% | `hm-darkgreen` |
| 蓝色边框 | 今天（叠加在颜色之上） | `hm-today` |

无图表库依赖，纯 CSS `background` 实现。暗色模式下自动切换深色调配色。

---

## 九、首页布局（移动端优先 · v5.0 驾驶舱重排）

```
0. AppBar（顶栏，仅首页 .home-only）
   ├── 字标：燕燕考研 · 备考系统
   ├── 倒计时 chip（距初试天数，点击改考期 = 原 changeExamDate）
   └── ⚙ 设置（打开设置抽屉）
1. Hero 问候卡（深墨绿，唯一着色焦点）
   ├── 时段问候 · 当前阶段
   ├── 动态状态文案（按今日完成度切换）
   ├── 🔥 连续打卡天数（暖橙）   +   今日进度环（SVG）
   └── AI 教练一句话建议条（点击进 AI 助手）
2. 阶段切换（在职 / 全职）
3. 周/月导航 + 今天按钮 + 起始日期设置
4. 今日任务卡片
   ├── 任务列表（含打卡复选框）
   ├── 昨日薄弱点提醒（自动，无记录则隐藏）
   ├── 偏科预警（自动，无预警则隐藏）
   └── 每日笔记（薄弱科目选择 + 文本输入 + 保存状态）
5. 统计卡片（本周完成率 / 连续天数 / 累计完成 + 近4周热力图 + 笔记回看）
6. 快捷入口 bento（学习中心 / 资料网课 / AI 教练）

设置抽屉（⚙ 打开，slide-over）：学习提醒 · 数据导入/导出/重置
  —— 这些"配置项"从首页主流移出，首页聚焦"今天怎么学"。
```

> **关键 ID/类**：首页专属元素统一加 `.home-only`，`showPage` 据此显隐；`#heroCard`（renderHero 填充）、`#homeQuick`（renderHomeQuick 填充）、`#appBar`、`#settingsMask/#settingsDrawer/#settingsBody`、`#reminderCard`/`#toolbarRow`（init 时 `appendChild` 进抽屉）。

---

## 十、已实现功能 & 待办

### ✅ 已完成（v4.0 驾驶舱版）

1. ✅ **PWA 支持** — manifest.json + Service Worker + 离线缓存
2. ✅ **距初试倒计时** — 颜色按紧迫度分级
3. ✅ **起始日期自动定位** — 启动时自动计算当天位置，可手动导航
4. ✅ **自定义任务** — 编辑模式增删任务
5. ✅ **数据导出/导入** — JSON 备份恢复（含 resources/studySessions/aiChats）
6. ✅ **每日薄弱点笔记** — 科目选择 + 自动保存 + 历史回看
7. ✅ **昨日薄弱点提醒** — 连续3天同科警告
8. ✅ **近4周热力图** — 5色方案 + 今天蓝色描边
9. ✅ **偏科预警** — 冷启动保护 + 红/黄分级 + 动作建议 + 数量限制
10. ✅ **资料包** — 新增/编辑/删除资源，按科目/类型筛选，进度追踪，点击打开链接
11. ✅ **学习会话** — 外部学习后确认卡片，同步勾选今日任务
12. ✅ **AI 助手** — 三种模式（答疑/错题/计划），mockChat 离线演示，后端代理注释
13. ✅ **底部导航** — 三 Tab 切换（打卡/资料包/AI助手）
14. ✅ **暗色模式 + iOS 适配**

### 📋 待办（后续迭代）

**学习中心引擎（框架已就位，按序填充）：**
1. ✅ **背单词**（v4.6 已上线）：多词库 + 斩词识别 + 艾宾浩斯 SRS + 错词本 + TTS + 导入词库。**待补**：完整考研大纲词表（公开词表转 JSON 导入）
2. ✅ **阅读**（v4.7 已上线）：精读 + 点词查义 + 一键加入背单词。**待补**：真题/外刊文章（导入）、阅读理解题目
3. ✅ **数学**（v4.8 已上线）：分类练习（微积分/线代/概率单选题）+ 即时判分 + 内置步骤讲解 + 错题本 + AI 详解（仅辅助）+ 导入题集。**待补**：396 历年真题（导入）、更细题型分类与难度筛选
4. **真题题库**：结构化真题（年份/科目/题型），作为模拟考与数学的内容底座
5. **AI模拟考**：作文/专业英语翻译批改 + 模拟卷
6. **今日学习**：聚合各引擎的今日任务（依赖 ≥2 个引擎就绪）

**其它：**
- 真题/模考成绩记录 + 趋势折线图
- 全职阶段"今日聚焦"精确到每日 list
- widget 版同步更新

---

## 十一、改动注意

- 旧 localStorage 键名保持 `kaoyan_v2`
- notes 使用 `YYYY-MM-DD` 绝对日期键，依赖 `startDate` 计算；未设置 startDate 则不显示笔记区域
- 修改任务数组长度时 `getDone` 自动补齐，不会丢失打卡数据
- 旧版 v1/v2 数据自动迁移，不丢数据

---

## 十二·补、设计系统（v4.9 起 · 重构中）

> 目标：从"工具 demo"升级为"高级、有质感、有陪伴感的考研备战系统"。遵循 minimalist-ui 为主、high-end-visual-design 克制取用。分阶段（P1 底座 → P2 首页驾驶舱 → P3 AI 教练 → P4 资料中心 → P5 打卡成就 + 移动端/PWA 收尾），每阶段独立可用、不破坏功能、单独提交。

### 设计令牌（CSS 变量，定义于 `:root`，含暗色推导）
- **品牌主色**：深墨绿 `--brand #1f4d3f`（暗色 `#2f7d62`）。**关键手法**：legacy `--color-text-info` 一并重指为墨绿——倒计时/主按钮/进度条/选中态/复选框/底栏/链接全部一处改全局。
- **强调色**：暖橙 `--ember #c2570f`（暗色 `#e08a4f`）+ `--ember-soft`，**稀缺使用**：连续天数、冲刺必看、关键鼓励。
- **中性**：暖白基底 `--color-background-tertiary #faf9f5`、卡面 `#fff`、次级 `#f1efe8`；统一发丝边框 `--hair`/`--hair-strong`。
- **阴影**：`--shadow-ambient`（卡片浮起）/`--shadow-key`（hero/弹层），带品牌色相、极低不透明，替代纯边框扁平感。
- **半径**：`--border-radius-sm 8 / md 10 / lg 14 / --radius-xl 20 / --radius-pill 999`。
- **缓动**：`--ease cubic-bezier(.16,1,.3,1)`、`--ease-out`。
- **字体**：`--font-sans`（系统栈）+ `--font-serif`（系统 serif 备选）。

### 排版
字重层级（400/500/600）、大标题负字距、**数据全量 `tabular-nums`**、`100dvh`、`-webkit-font-smoothing`。

### 组件库（P1 新增，供后续阶段复用）
`.eyebrow`（微标签）· `.btn-primary/.btn-ghost` · `.btn-cta`（含 `.cta-ico` button-in-button 尾部圆形图标）· `.chip`+`.chip-row`（快捷问题/筛选，横滑）· `.badge` ×5 状态（`.b-read/.b-new/.b-rec/.b-freq/.b-sprint`）· `.ring-wrap`（SVG 进度环）· `.empty-state` · `.skeleton`（骨架屏）· `.reveal`（滚动入场，配 IntersectionObserver）。

### 交互态
`:focus-visible` 焦点环、`active:scale` 按压物理、`@media(hover:hover)` 悬浮抬升、`@media(prefers-reduced-motion)` 守卫。

### 关键设计取舍
- **字体刻意保留系统栈**：两个设计 skill 都建议换字符字体，但本项目是**离线优先 PWA + 中文为主 + 历史"去 CDN"原则**。加载 Web 字体会破坏离线、且对中文字形几乎无效；改为在系统栈内用字重/字距/tabular-nums/serif 备选拉开质感。如需 Latin 展示字体，后续可自托管 woff2 + SW 预缓存。
- **拒绝**：玻璃拟态滥用、大旋转、营销级 `py-40` 动效、`rounded-full` 大容器。**取用**：自定义缓动、按压物理、嵌套层级、tinted 阴影、eyebrow、button-in-button。
- **emoji**：结构 UI 逐步替换为线性 SVG；人声文案（鼓励/连续天数）保留极少量以保陪伴感。

---

## 十二、变更日志

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-06-30 | v8.3 | **Edge 神经语音朗读（客户端直连 + 系统语音兜底）**：阅读/单词/例句优先用微软 Edge 神经语音（`en-US-AriaNeural`/英 `en-GB-SoniaNeural`），接近真人。**关键：edge-tts 封数据中心 IP**（实测服务端 403，Netlify/Lambda 同样不可用），只有住宅 IP 能用，故在**浏览器端直连** WSS（`crypto.subtle` 算 `Sec-MS-GEC` 签名 → `speech.platform.bing.com` → 收 MP3 → `Audio` 播），免 Key 免费。统一 `speak({lang,rate,onend})`，阅读逐句 + 预取下一句；失败/离线/被拦截自动回退 `speechSynthesis`，连续 3 次失败本会话停用。设置抽屉新增「朗读语音」开关 + 「试听一句」自检（存 `localStorage.tts_neural`）。SW 升 v19。**注**：音质能否生效取决于用户所在网络是否被微软放行——需在真机/家庭网络用「试听一句」验证；若被拦回退系统音，则改走 Azure 神经语音免费版（同音色，需免费 Key）。预览（数据中心 IP）只验证了 API 装配与回退正常，神经音真机自测。 |
| 2026-06-30 | v8.2 | **首页精简 + 修顶部留白 + TTS 更自然**：① 删除首页快捷入口 bento（`#homeQuick`：学习中心/资料网课/AI教练，与底栏重复）。② **修 iPhone 顶部大片留白**——`--safe-top` 原在 `body`(safe-top+12) 与 `.appbar`(padding-top:safe-top) 各加一次=双倍安全区；移除 appbar 的 padding-top、body 顶 padding 收为 `safe-top+6px`。③ AppBar 倒计时与设置**位置互换**（`.ab-actions` → `row-reverse`）。④ **TTS 选最自然嗓音**——新增 `_pickVoice(lang)`：按 natural/neural/online/premium/enhanced 及 aria/jenny/samantha 等关键词挑高品质英文音色（在线嗓音优先），`onvoiceschanged` 重置缓存；`vSpeak` 与阅读逐句试听统一用之，rate 0.96 / pitch 1.02。**注**：最终音质受设备已装语音限制，iPhone 建议在 设置→辅助功能→朗读内容→声音→英语 下载"增强版"；要彻底自然需接云端/边缘神经 TTS（另议）。移动端预览验证：bento 消失、顶部留白收紧、设置/日期已互换。 |
| 2026-06-30 | v8.1 | **学习中心分组 + 配色调柔/渐变 + 数学课程增强**：① 学习中心模块卡按 `group` 分「英语/数学/综合」三段（`.lc-section` 标题，整行跨列），移除「可用」徽章（未完成仍留「敬请期待」+淡化 `.lc-soon`）。② 品牌蓝调柔 `#0070E0→#1A6FD4`，新增 `--brand-gradient`/`--brand-gradient-soft`，首页 hero 与 `.btn-cta` 改柔和蓝色渐变（去纯色块刺眼感）。③ 数学课程播放器加 **倍速** `mcRateSeg/mcSetRate`（0.75–2×，存 `mathcourse.settings.rate`，挂载时 `applyRate`）、**课堂笔记** `progress[id].note`（防抖自动保存）、**导出备份** `mcExport`（下载课程+进度+笔记 JSON）+ `mcDoImport` 支持粘贴备份对象恢复。移动端预览验证：分组三段/无可用徽章、hero 渐变、倍速选中/笔记框/备份按钮均通过。 |
| 2026-06-30 | v8.0 | **主题换肤：PayPal 蓝/白**：品牌色墨绿→PayPal 蓝（`--color-primary #0070E0`/hover `#0059B3`/软 `#E5F0FB`），所有走 `--color-primary`/`--brand`/`--color-text-info` 的控件（按钮/进度条/复选框/选中态/底栏/链接，91 处）一次性变蓝；首页 hero 深色容器 `--color-surface-deep` 改 PayPal 深蓝 `#003087`；硬编码墨绿（`.t-eng/.t-write` 标签底、长难句"主语"高亮、`learn-card` 边框、页面环境光 radial）全部换蓝；深色模式品牌色改亮蓝 `#409CFF`；PWA `manifest.theme_color`→`#0070E0`、`icon.svg` 渐变→`#003087→#0070E0`。语义绿/红、暖橙强调（错词/streak）保留。移动端预览验证：hero 深蓝、主按钮/进度条 `rgb(0,112,224)`、底栏选中蓝。 |
| 2026-06-29 | v7.9 | **数学课程模块（自托管直链视频·章节·续播）**：学习中心新增「数学课程」卡 → `pageLearnMathCourse`。① 应用内 `<video>`/HLS(hls.js) 内嵌播放 `.mp4`/`.m3u8` 直链（复用 `detectPlayMode`/`loadHls`）。② 章节分组 + 进度（已学N/总数、单节%、续播「上次看到」）+ 上/下一节 + 标记已学；切页自动暂停 `_mcStop`。③ 「添加」单节 + 「导入」批量 JSON（`{chapter,title,url}`），课节存 `state.learn.mathcourse`，可选静态 `data/courses/math.json`。④ 粘贴链接即时校验，**百度网盘/外链无法内嵌**会明确提示+「新窗口打开」，卡片附「网盘视频转直链」指引（下载→对象存储公共读→直链；HLS 需配 CORS）。**说明**：百度网盘分享页禁 iframe 嵌入、直链登录绑定且短时失效，故无法直接内嵌，需转直链自托管（用户已确认走此方案）。移动端预览验证：添加课节→章节分组→播放器挂载 `<video>`(直链)→上/下一节/标记已学均通过。 |
| 2026-06-29 | v7.8 | **内置词义详解库（词根+例句·离线零 Token）**：① 新增 `scripts/gen-word-details.mjs`——走已部署 gen 代理分批（默认15词/批，`maxTokens`可调）生成 5500 词的「词根助记+地道例句+翻译」，落盘 `data/words/kaoyan-5500-detail.json`，即时落盘+断点续跑（`node scripts/gen-word-details.mjs`，可加 `--limit`/`--batch`）。② `chat.js` gen 模式支持 `maxTokens`（200–4000，默认600）。③ 前端 `loadWordDetail()` 懒加载详解库（进背单词/阅读时预载）；`openWordCard` 优先用内置详解（词根/例句零 Token），查不到才按需 AI，仅「未收录释义」冷门词自动补。释义早已内置（`splitDefSenses`），加上本库后绝大多数词的释义+词根+例句全部离线零成本。 |
| 2026-06-29 | v7.7 | **释义内置省 Token + 答错直弹详解**：① 删除答题后冗长反馈块（再记一次/生成例句/已加入错词本），只留「词义详解」+「下一个」；**答错自动弹词义详解卡**（`showVocabFeedback`/`showVocabSpellFeedback` 末尾 `if(!correct) vocabWordCard(...)`）。② **释义零 Token 内置**——新增 `splitDefSenses(def)` 按词性（n./v./vt./adj.… 用  标记切分）把词库 def 拆成「全部释义」多行，5500 全词释义都来自内置词库不花 Token；`renderWordAnalysis` 当已有内置 def 时跳过 AI「全部释义」防重复。③ **冷门词才 AI**——`openWordCard` 仅当「无内置释义且无缓存」时才自动调一次 `readingWordAnalyze`（永久缓存 `gen_wd_<word>`）；AI 按钮收窄为「AI 词根+例句」。阅读点词同理：收录词秒显内置释义，未收录才 AI。移动端预览验证：拼写答错→反馈块消失、词义详解自动弹出、accelerate 显示「v.使加速…/vi.加快…」双行内置释义、美英发音+AI按钮齐全。 |
| 2026-06-29 | v7.6 | **查词卡通用化（背单词也能用）**：把阅读的百词斩式查词卡抽成通用组件 `openWordCard(entry,opts)` + 共享浮层 `#wordCard`（移出阅读页 DOM，置于页面外层，任意页可弹）。① 阅读点词走 `readingTap→openWordCard({showSave:true})`；② 背单词侧新增 `vocabWordCard(lib,word)`——斩词/拼写答完的反馈卡、「已学单词浏览」每行都加「词义详解」入口，点开同款富卡片（美/英发音 + 释义 + 例句 + AI 详解），背单词侧不显示「加入背单词」、不自动朗读。复用 `gen_wd_<word>` 缓存与 `gen_ex_<lib>:<w>` 例句缓存。移动端预览验证：背单词反馈→词义详解→卡片（音标/美英/释义/例句/AI详解）正常。 |
| 2026-06-29 | v7.5 | **阅读逐句精读 + 查词卡展开解析**：① **逐句渲染**——文章按句切分（`rdSplitEn`/`rdSplitCn`），每句独立成块、自带 🔊；显示译文时内置 3 篇逐句中英对照（手写段译按句切分与英文句数对齐则自动逐句对齐，否则回退整段）。② **试听改逐句**——`_rdPlayer.units` 句级单元，全文逐句连读+高亮当前句，点句 🔊 从该句起读。③ **查词卡（百词斩式）**——点单词弹富卡片：大词+音标+**美/英双发音**（`vSpeak(t,lang)` en-US/en-GB）+释义+例句；新增「AI 详解」调 DeepSeek 生成**全部释义(分条)/词根助记/地道例句+翻译**，缓存 `gen_wd_<word>` 秒开（不编造词频/配图）。新增 `rdSplitEn/rdSplitCn/renderWordAnalysis/parseWordCard/readingWordAnalyze`；`.rd-sent/.rw-*` CSS；`.reading-pop` 可滚动。移动端预览验证：逐句中英对照、逐句试听高亮、查词卡美英发音+AI详解入口均通过。 |
| 2026-06-29 | v7.4 | **极简高级 UI 改版（去 emoji + 背单词主页重排）**：① **全量 SVG 图标**——全站布局 emoji 换成既有 `I` 线性图标集（单一图标语言），新增 `sound/play/stop/refresh/bookOpen/arrowLeft/x/xCircle/bookmark/pen/layers/grid/sigma/robot`。② **发音按钮重做**——🔊 emoji → 极简圆形描边图标（大号 `.vocab-speak` + 行内 `.spk`），覆盖单词卡/拼写/例句/已学浏览/阅读逐段/查词弹层。③ **背单词主页**——`.fill-card` 撑满视口消除底部留白；新增「开始复习」（只练到期词，`buildVocabQueue('review')`）；每日新词/错词本/已学改为更小、钉底的次级按钮；主操作升级 `.btn-cta`。④ **学习中心 hub** 10 卡图标全换线性 SVG。⑤ **反馈/完成态**——`fbHead()`（图标+语义色替代 ✅❌）、完成页对勾、错词提示书签、偏科预警语义色圆点。⑥ 长难句/作文/数学/错题/AI 训练横幅/仪表盘/推送设置同步替换或移除 emoji；保留排版箭头 `← →` 与极简对勾 `✓`。移动端预览验证：背单词主页填满屏 + 复习入口、阅读试听/译文、发音图标、hub 图标均通过。 |
| 2026-06-29 | v7.3 | **背单词·例句/复习 + 阅读·试听/译文（四项体验补全）**：① **例句朗读**——复习反馈卡英文例句旁加 🔊（`speechSynthesis`，离线）；用 `data-say` 属性承载句子文本避免 onclick 转义问题（新增通用 `vSpeakEl(el)`）。② **AI 按需补例句**——词库本身无例句时（如 5500 词库）显示「✨ 生成例句」，调 DeepSeek 生成「例句+中译」并缓存 `localStorage['gen_ex_<lib>:<w>']`，下次秒开、失败可重试；新增 `aiGen/genCacheGet/genCacheSet/vResolveEx/vExBlockHtml/vocabGenExample`。③ **已学单词浏览页**——词汇主页新增「📖 已学单词浏览(N)」，按 待复习/学习中/已掌握 筛选，逐词看释义·例句·发音，「重新复习」把 `progress.due` 设回今天重排队列；新增 `vocabBrowse/vbToggleEx/vbReview`。④ **阅读试听**——文章顶「🔊 试听全文」逐段连读 + 高亮当前段（`SpeechSynthesisUtterance.onend` 链式推进），每段末 🔊 可从该段起读；新增 `readingPlayToggle/readingPlayFrom/readingPlayNext/readingStopPlay/readingHighlight`。⑤ **段落中译**——内置 3 篇文章 JSON 加平行 `cn[]`（手写高质量中译），「显示/隐藏译文」一键切；导入文章缺译文按段「✨ 翻译这段」AI 生成并缓存 `gen_rcn_<id>:<i>`；新增 `readingResolveCn/readingCnHtml/readingToggleCn/readingGenCn`。⑥ **后端**——`netlify/functions/chat.js` 新增 `mode:gen`：跳过陪跑系统提示、低温度(0.3) 返回纯净文本，专供内容生成。本地 5500 词预览验证：阅读试听/译文、例句朗读、生成按钮、浏览页全链路通过。 |
| 2026-06-25 | v6.5 | **作文模块 P5（AI 批改 + 范文 + 模板）**：训练舱新内容引擎 #2，考研英语二作文 25 分核心提分点。① **数据**：`data/writing/` 注册表 + 题目库 `kaoyan-eng2-prompts.json`（4 题：道歉信/推荐信小作文 + 图表/观点论述大作文，含 prompt/requirements/高分范文 sample/范文亮点 sampleNotes）+ 模板库 `kaoyan-eng2-templates.json`（6 组 25 句高频开头/主体/结尾/过渡句型）。② **写作流**：题目列表（小/大作文徽章 + 已批改/草稿状态）→ 题干+要求 → textarea（实时英文词数统计 + 自动存草稿）→「🤖 AI 批改」→ 看范文 / 模板库。③ **AI 批改**：构造阅卷 prompt → 调 `API_ENDPOINT` → `parseWritingGrade` 解析结构化 JSON（评级 S/A/B/C/D + 得分 + 总评 + 优点/问题/建议三栏，稳健去围栏）→ 渲染评分徽章（高分墨绿/低分暖橙）+ 三色反馈块；AI 不可用时降级为「对照范文找差距」引导。④ **接脊柱**：批改提交 `logEvent('writing','submit',xp:10)`；低档（C+/C/D 或<60%）→ `recordMistake`，高档 → `resolveMistake`；批改历史存 `state.learn.writing.history`。⑤ **范文对照**：高分范文 + 亮点拆解（墨绿讲解卡）。⑥ **模板库**：6 组句型，考场套用。数据校验通过；E2E smoke 通过（题目列表/写作/词数/范文/模板 + AI批改JSON解析+评分渲染+脊柱高分销账）。新增 `data/writing/*` + 14 个 writing 函数 + `.wr-*` CSS。学习中心 hub 新增「✍️ 作文」模块（ready）。SW 升 v16。 |
| 2026-06-25 | v6.4 | **长难句模块 P4（考研英语二真题难句·成分拆解·AI讲解）**：训练舱新内容引擎 #1。① **数据**：`data/syntax/index.json` 注册表 + 种子 `kaoyan-eng2-core.json` 共 6 句考研英语二真题风格难句，覆盖最高频考点（同位语+定语从句、三层定语嵌套、过去分词作状语、it 形式主语、while 对比并列、it is...that 强调结构）。每句含 `en`/`cn`/`parts[]`（按顺序的成分拆解片段）/`tip`（拆解思路）/`trap`（易错点）；parts 串接严格等于 en（验证通过）。② **6 色成分高亮**：主语(墨绿)/谓语(橙)/宾表(蓝)/定语-从句(紫)/状语(青)/同位语(棕黄)/标记词(灰)，含暗色模式适配；title 属性显悬浮注释。③ **UI 教学流**：先看原文 → 自己断句 → 「展开成分拆解」(避免剧透) → 看高亮+图例 → 翻译 → 拆解思路（墨绿卡）+ 易错点（暖橙卡）→ ✓已掌握 / ✗还不懂 / 🤖AI详解。④ **接脊柱**：「已掌握」→ `logEvent('syntax','mark',correct:true)` + `resolveMistake`；「还不懂」→ `logEvent(correct:false)` + `recordMistake`。⑤ **AI 详解**：把句子+引导问题填入 AI 助手并跳转。⑥ 句库选择器（多句库切换）+ 已掌握进度条 + 列表卡片（cat 徽章 + 状态徽章）。新增 `data/syntax/*` + 11 个 syntax 函数 + `.syn-*/.sp[data-r=*]` CSS。学习中心 hub 新增「🔍 长难句」模块（ready）。SW 升 v15。 |
| 2026-06-25 | v6.3 | **跨模块错题中心（训练舱 P3）**：学习中心 hub 新增「📌 错题中心」模块（ready）。① **统一视图**：把 vocab/math/reading 各自分散的错题/错词，从脊柱 `listMistakes()` 一锅端展示——统计卡（总未解决 / 错词 / 错题）+ mod 筛选 chip（全部/背单词/数学，按是否有数据动态显隐）+ 错题列表（mod 徽章 + 标题 + 分类 + 暖橙「错 N 次」+ 最近日期 + 删除）；② **一键再练**：`mistakesStartPractice` 取筛选后最多 8 条 → `buildMistakesTrainingPack` 按词库/题集分组查回原始内容、构造 MCQ（复用 `vDistractors` 干扰项）→ 写入 `state.learn.lastPack`(source:'mistakes') → `showPage(2)` 跳 AI 训练舱，由 P1 训练引擎 `renderAI` 自动 resume 接管——**所有训练都在统一训练驾驶舱发生**，答对达 master 自动销账；③ **管理**：单条删除=`resolveMistake`（不动词库/题库本身，仅停止追踪）；④ 空状态/筛选空态分别引导。新增 `openMistakes/renderMistakesHome/mistakesSetFilter/mistakesRemove/mistakesStartPractice/buildMistakesTrainingPack`；新增 `.mist-*` CSS。E2E smoke 通过：列表渲染/筛选/一键再练→跳 AI 页→pack 装载→4 选项答题全链路。SW 升 v14。 |
| 2026-06-25 | v6.2 | **训练舱仪表盘（P2）**：把脊柱数据接进首页 Hero——从「打卡进度环」升级为「训练舱驾驶舱」。① **三连 quest 进度环**：背单词/精读/数学（54px 迷你环 + 数值 cur/goal），数据来自 `getDailyTraining()`；② **训练 streak**（暖橙火苗 + 连续训练天数）替代原打卡 streak（打卡数据保留在下方 stats 卡，未丢）；③ **XP/Lv 进度条**（同一行右侧 flex:1，含 Lv 标 + 今日 +N XP + 当前/200 进度，每 200 XP 升一级）；④ **状态文案** `trainingStatus(t)`：按训练进度三态切换（未开始 / 中途差几个达标 / 全部达标 ✓）；⑤ **AI 教练条**优先级升级：先看薄弱点 top1（≥50% 错率显示「kaoyan 错 50% — 让 AI 出针对性练习」），再看错题数（≥3 条），最后回退到打卡偏科文案；⑥ **兜底**：脊柱数据不可用时自动退回到旧打卡进度环渲染，零回归风险；⑦ `ringSVG(pct,size,mini)` 加 mini 模式（细描边、隐藏"今日"小字）；新增 `.h-quests/.hq-ring/.h-foot/.h-xp/.h-streak-inline` CSS。**首页第一眼即"训练舱"感**：用户做完一题就看到 quest 环增长、XP 涨、streak 续——P0/P1 的脊柱终于在最显眼处亮相。 |
| 2026-06-25 | v6.1 | **AI 自适应今日训练（训练舱 P1）**：把脊柱「点亮」——AI 教练页新增「🎯 生成今日训练」横幅。① **画像种子** `getTrainingSeed()`：读 `topWeakness('byCat'/'byMod')` + `listMistakes()` 真实错题标题；② **在线路径**：`genTrainingPrompt` 构造严格 JSON Prompt（考研英语二+396 风格、紧扣薄弱点、6 道 MCQ）→ 调现有 `API_ENDPOINT` → `parseTrainingPack` 稳健提取 JSON（去 markdown 围栏、截取 `{...}`、字段校验）；③ **离线兜底** `buildOfflineTrainingPack`：AI 不可用时从 `vocab.wrongBook`(错词，复用 `vDistractors` 生成干扰项) + `math.wrongBook`(错题) 抽题重练，**离线优先 PWA 也能用**（先 `loadVocabLibs/loadMathSets` 加载注册表再取词/题）；④ **答题回流脊柱**：每题 `logEvent` + 命中已有错题则 `resolveMistake`/`recordMistake`，训练结果计入今日 quest/XP/错题本；⑤ **会话持久化** `state.learn.lastPack`：未完成可下次进 AI 页自动续做；⑥ 横幅三态（无数据引导→有薄弱点显示「错N%」chips+生成按钮→生成中骨架屏）；训练卡复用 vocab/math 卡片样式 + `.train-*` 新样式。新增 `getTrainingSeed/genTrainingPrompt/parseTrainingPack/buildOfflineTrainingPack/renderTrainBanner/generateTraining/startTraining/renderTrainCard/trainPick/showTrainFeedback/trainNext/renderTrainDone/quitTraining/finishTraining`。运行时 smoke test 通过：离线生成→渲染→答题→反馈→脊柱回流全链路。纯前端、不引入框架/后端依赖。 |
| 2026-06-25 | v6.0 | **训练舱脊柱 P0（统一学习事件 + 错题/错词 + 薄弱点 + XP/Quest）**：从「一堆小工具」升级为「考研英语 AI 训练舱」的**核心地基**。① **数据模型 v8**（向后兼容，v7→v8 自动迁移）：`state.learn` 新增 `log[]`(append-only 学习事件流，硬上限 5000)、`mistakes{}`(跨模块错题/错词索引，键`${mod}:${refId}`)、`weakness{byMod,byTag,byCat}`(14 天滚动薄弱点)、`daily{xp,quests,streak,lastDate}`(每日 quest+XP+连续，跨天重置)、`profile{level,totalXp,examTarget}`(等级系统)。② **脊柱 API**（全部 `window.*` 暴露）：`logEvent(mod,action,ev)` 单一入口、`recordMistake/resolveMistake`、`recomputeWeakness`、`topWeakness(dim,n,minTotal)`、`listMistakes(opts)`、`getDailyTraining()`。③ **三个引擎接入**（最小改动，仅在 grade/mark 时加 try{} 调用）：背单词 `gradeVocab` 写 vocab 答题事件 + 错词销账（达 master stage 时）、数学 `gradeMath` 写 math 答题事件 + 错题销账、阅读 `markPassageRead/addWordToReadingLib` 写 reading 标记/加生词事件。④ **XP 表**：`vocab_correct=2 / vocab_wrong=1 / math_correct=6 / math_wrong=2 / reading_done=8 / writing_submit=10`；每 200 XP 升一级。⑤ **Quest 表**：vocab≥20/reading≥1/math≥5 任一达标即续 streak。⑥ 借鉴：WordPecker 的 5 题型架构、LinguaCafe 的词汇分级、LibreLingo 的关卡门控、LLPlayer 的双字幕范式 —— 均落到本项目的「单文件 vanilla-JS + 离线优先 + localStorage」栈，**不引入框架/后端依赖**。运行时 smoke test 通过：xp/streak/quest/mistake/weakness 全维度验证。**未动现有 UI**，AI 自适应训练 + 训练舱仪表盘下一步实现。 |
| 2026-06-25 | v5.1 | **AI 学习教练页（重构 P3）**：从裸聊天框升级为教练。① **教练状态头**（头像 + 在线绿点 + 动态状态"在线·今天已答 N 题"）；② **快捷问题 chips**（按模式分组 5 条，点击自动填入+发送）：「今天学什么」「坚持不住了」「复盘」「推荐资料」「调整计划」等，复用 `.chip` 组件；③ **sticky 底部输入条**(`position:fixed` 于底栏上方，修复手机要滚到底才能打字 + 键盘体验，**解决 AI 页底部异常空白** pain point）—— textarea 自动高度 + 软键盘弹起时 `body.ai-typing` 微调布局；④ **结构化回答**：`renderAIReply` 识别「结论:」「当前问题:」「今天怎么做:」「别犯的错:」「一句鼓励:」等标题，包成带标签的视觉卡块 `.ai-block` × 5 种样式(ab-conclusion/issue/action/warn/cheer)，手机阅读更友好；`sendAIMsg` 向后端传 `formatHint` 提示五块格式（兼容现有后端）；⑤ 空状态：首次打开显教练简介卡；⑥ mode 切换刷新 chips；updateAIStatus 动态显今日问答数。新增 `renderAIQuickChips/updateAIStatus/aiAskQuick`，textarea auto-grow + focus/blur → `.ai-typing` class。视觉升级：coach 头卡 + 五色结构块 + sticky 输入条 + 绿色 online dot。纯前端零回归（运行时校验通过）；SW 升 v11 |
| 2026-06-25 | v5.0 | **首页驾驶舱（重构 P2）**：把首页从"配置面板"重排为"学习驾驶舱"，不动 `state`/后端/数据。① 顶部 **AppBar**（字标「燕燕考研·备考系统」+ 倒计时 chip(原 `countdownDays`/`changeExamDate` 迁入，点击改考期) + ⚙ 设置）；② **Hero 问候卡**（深墨绿渐变唯一着色焦点）：按时段问候 + 阶段 + 动态状态文案 + 🔥连续天数(暖橙火苗) + **今日进度环**(SVG `ringSVG`) + AI 教练一句话建议条(规则生成 `heroCoachTip`，点击进 AI)；③ **快捷入口 bento**：学习中心(宽)/资料网课/AI教练；④ **设置抽屉**(`slide-over`)：把「学习提醒」「数据导入导出」从首页主流 `appendChild` 迁入，⚙ 打开——首页只剩"今天怎么学"。新增函数 `renderHero/renderHomeQuick/ringSVG/greetingByHour/heroStatus/heroCoachTip/initSettingsDrawer`；`showPage` 选择器加 `.home-only` 统一首页元素显隐；`window.showPage` 暴露供内联调用。纯前端、零功能回归（运行时校验：节点迁移/hero 渲染/bento 均通过） |
| 2026-06-25 | v4.9 | **设计系统重构 P1（底座）**：从"工具感"升级为"备考系统"的第一步，纯 CSS、零功能改动。① 品牌色——深墨绿 `#1f4d3f`（复用 legacy `--color-text-info` 一处改全局，所有主控件：倒计时/主按钮/进度条/选中态/复选框/底栏一次性变绿）+ 暖橙 `--ember #c2570f`（连续天数/冲刺/鼓励的稀缺强调）；② 暖中性灰阶 + 统一发丝边框 `--hair`；③ tinted 阴影（带品牌色相、极低不透明 `--shadow-ambient/--shadow-key`）替代纯边框扁平感；④ 半径尺度(sm8/md10/lg14/xl20/pill) + 缓动 `--ease cubic-bezier(.16,1,.3,1)` + 系统 serif 备选 `--font-serif`；⑤ 排版升级：字重层级、负字距、数据全量 `tabular-nums`、`100dvh`、字体平滑；⑥ 新增组件库（供 P2-P5）：`.eyebrow`/`.btn-primary/.btn-ghost/.btn-cta`(button-in-button)/`.chip`+`.chip-row`/`.badge`×5状态(已读/未开始/推荐/高频/冲刺必看)/`.ring-wrap`(进度环)/`.empty-state`/`.skeleton`/`.reveal`(滚动入场)；⑦ 交互态：`:focus-visible` 焦点环、`active:scale` 按压物理、`@media(hover)` 悬浮抬升、`prefers-reduced-motion` 守卫；⑧ 底栏改为半透明 `backdrop-blur` 浮层。**设计取舍**：遵循 minimalist-ui 为主 + high-end-visual-design 克制取用（自定义缓动/按压物理/嵌套层级/eyebrow/tinted阴影），拒绝玻璃拟态滥用/大旋转/营销级动效；**字体刻意保留系统栈**（离线优先 PWA + 中文为主 + 项目"去 CDN"原则，详见设计系统章节） |
| 2026-06-25 | v4.8 | **数学引擎（396 分类练习）**：按题集（微积分/线代/概率）做单选题 + 即时判分 + 内置步骤讲解(`.math-explain`) + 错题本重做(连续答对达 `MATH_MASTER=3` 自动移出) + 「AI 详解」一键把题目填入 AI 助手「错题讲解」模式(AI 仅辅助)；题集选择 + 导入题集(粘JSON)；附 3 个种子题集 `data/math/{calc,linear,prob}.json`(各 8 题含讲解) + 注册表 `index.json`(逻辑占位)；进度走 `state.learn.math`(active/progress/wrongBook/daily/userSets，ensureMath 防御补齐)、内容走静态JSON/localStorage；复用 vocab 卡片样式、无 MathJax 依赖；SW 升 v9 |
| 2026-06-24 | v4.7 | **阅读引擎（薄荷阅读底层逻辑）**：精读文章 + 点词查义弹层(释义+TTS) + 一键加入背单词(写入 `vocab_lib_reading` 并注册「阅读生词本」进 SRS) + 已读/生词进度；导入文章(粘JSON)；附 2 篇原创短文(国际贸易/跨境电商) + 注册表 `data/reading/index.json`；SW 升 v8 |
| 2026-06-24 | v4.6 | **背单词引擎（百词斩底层逻辑）**：斩词识别(4选1) + 艾宾浩斯间隔[0,1,2,4,7,15,30,90,180]天 + 每日新词配额 + 熟词跳过 + 错词本；多词库选择 + 导入词库(粘贴JSON)；TTS 发音(Web Speech)；种子库 `data/words/kaoyan.json`(考研核心50词) + 注册表 `index.json`(四级/六级/专英占位)；词库内容走静态JSON/localStorage、进度走 state.learn.vocab；SW 升 v7 |
| 2026-06-24 | v4.5 | **学习中心框架**：`资料包` Tab 改名 `学习`，升级为 hub 卡片网格 + tab 内子路由（`showLearnView`）；7 个模块卡（今日学习/背单词/阅读/数学/真题题库/AI模拟考/资料网课），`资料网课` 复用查看器、其余占位「敬请期待」；数据模型新增 `state.learn`（v7，`defaultLearn()`+迁移防御），内容/进度分离存储原则；引擎逐个填充不动框架 |
| 2026-06-24 | v4.4 | **资料包在线观看**：新增应用内查看器，按链接类型自动路由——`.mp4/.webm` 直链内嵌播放（记忆进度、续播、算百分比）、`.m3u8` 懒加载 hls.js、PDF 内嵌、Word/PPT 走 Office Online 预览、图片内嵌、B站内嵌播放器；网盘链接跳转并自动复制提取码；数据模型新增 `panCode`（提取码）与 `progress.videoTime`（续播秒数）；卡片增加 `▶ 在线播放`/`↗ 跳转` 角标；SW 缓存升至 v6 |
| 2026-06-23 | v4.0 | **驾驶舱版**：三页底部导航（打卡/资料包/AI助手）；资料包 CRUD + 筛选 + 进度追踪；学习会话半自动确认；AI 助手三种模式 + mockChat + 后端代理注释；自动定位启动时计算当天位置；数据模型新增 resources/studySessions/aiChats |
| 2026-06-23 | v3.2 | **打磨版**：偏科预警冷启动保护；排除 t-rev；最多2条预警+折叠；第1周不显示两周警告；统计零值鼓励文案；预警优先级排序 |
| 2026-06-23 | v3.1 | **反馈闭环版**：notes 重构为 YYYY-MM-DD 日期键 + weakSubject 科目字段；热力图升级为5色方案；偏科预警新增红/黄分级、动作建议文案、两周结构警告；新增昨日薄弱点提醒（含3天连续同科警告）；布局重组 |
| 2026-06-23 | v3.0 | 新增每日薄弱点笔记、近4周热力图、偏科预警 |
| 2026-06-23 | v2.0 | PWA 升级：manifest.json/sw.js/icon.svg；倒计时、起始日期定位、自定义任务、导出导入；去除 CDN 依赖 |
| 2026-06 | v1.0 | 初版 打卡工具.html：双阶段打卡、统计、重规划建议 |
