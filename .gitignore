# OS files
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo

# Old version (keep for reference but don't deploy)
# 打卡工具.html is kept for local use

# IDE
.vscode/
.idea/

# Netlify 手动拖拽部署用的临时干净目录（构建产物，不入库）
_deploy/

# Node / Vite（V7.0 起）
node_modules/
dist/
.vite/

# 环境变量（含密钥，绝不入库）
.env
.env.local
.env.*.local

# 测试 / 覆盖率
coverage/
*.log
